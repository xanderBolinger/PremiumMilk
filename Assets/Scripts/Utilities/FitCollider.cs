using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering;

public class FitCollider
{
    /*public static void FitColliderToChildren(GameObject formationGameObject)
    {
        var collider = formationGameObject.GetComponent<BoxCollider>();

        var bounds = new Bounds(formationGameObject.transform.position, 
            formationGameObject.GetComponent<FormationManager>().GetBounds());

        collider.size = bounds.size;
    }*/
}

